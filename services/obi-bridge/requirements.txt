# Pinned for reproducible Docker builds (recruiter-5).
# Pinned to 2.x (API-stable, matches the floor this service already targeted)
# so a silent 3.x major jump can't change consumer/producer semantics.
kafka-python==2.3.2
websocket-client==1.9.0
