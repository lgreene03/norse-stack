# Pinned for reproducible Docker builds (recruiter-5).
# Mimir is otherwise stdlib-only (http.server + sqlite3); kafka-python is the
# sole runtime dependency. Pinned to the same 2.x line odin/obi-bridge target
# so a silent 3.x major jump can't change consumer semantics under us.
kafka-python==2.3.2
