# Pinned for reproducible Docker builds.
# Forseti is otherwise stdlib-only; kafka-python is the sole runtime dependency.
# Pinned to the same 2.x line Odin and obi-bridge target so a silent 3.x major
# jump can't change consumer semantics under us.
kafka-python==2.3.2
